[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: Smoke tests for sanity checks
    unit: Unit tests for individual modules
    integration: Integration tests for module interactions
    e2e: End-to-end tests
